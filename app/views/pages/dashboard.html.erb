<!-- Change to current_developer -->
<% @id = Developer.last.id %>
<% projects = Project.all.where(user_id: @id) %>
<!-- Change to current_developer -->
<h1><%= "Hi, #{Developer.last.first_name} #{Developer.last.last_name}" %></h1>
<!-- Change to current_developer -->
<h4><%= Developer.last.email %></h4>

<h2>Your projects</h2>
<!-- Add path to update profile -->
<%#= link_to "Update profile", edit_user_registration_path(current_user) %>


<h2>Your proposals</h2>

